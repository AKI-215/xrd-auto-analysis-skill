# XRD plotting support

这份文档是给 `xrd-auto-analysis` skill 预留的作图能力草案，覆盖三类核心输出：

1. 单谱图 + 自动峰位标注
2. 多谱图 stacked plot
3. 样品谱 + 候选相参考峰位覆盖

## 1. 单谱图 + 自动峰位标注

### 输入
- 一个 SmartLab TXT / CSV 文件
- 可选：样品标签
- 可选：是否归一化
- 可选：峰检测阈值

### 输出
- 一张基础 XRD 谱图 PNG
- 横轴：2θ (°)
- 纵轴：Intensity (a.u. 或 cps)
- 自动标出主峰位置
- 可选输出峰表 CSV

### 默认规则
- 自动识别两列 XY 数据
- 默认保留原始 2θ 范围
- 默认绘制原始曲线；如用户要求比较，则归一化
- 自动峰位标注只标主峰，不把弱噪声峰全部打出来
- 峰位标注默认使用文本或短竖线，不默认使用图标

## 2. 多谱图 stacked plot

### 输入
- 多个 SmartLab TXT / CSV 文件
- 每条曲线对应标签
- 可选：统一 x 范围
- 可选：颜色表
- 可选：是否归一化

### 输出
- 一张 stacked plot PNG
- 每条曲线归一化并纵向错位
- 右侧或末端带标签

### 默认规则
- 自动取多条曲线共同存在的 2θ 区间
- 默认归一化到 [0, 1]
- 默认纵向错位，避免曲线互相遮挡
- 标签尽量放在右侧空白区
- 用于对比处理前后、不同腐蚀时间、不同温度等样品

## 3. 样品谱 + 候选相参考峰位覆盖

### 输入
- 一条样品 XRD 曲线
- 一组候选相参考峰位
- 可选：候选相名称
- 可选：峰位类别（基体 / 腐蚀产物 / 待验证）

### 输出
- 样品谱 PNG
- 参考峰以竖线、短 tick 或底部 marker 覆盖到图上
- 图例区分不同候选相

### 默认规则
- 参考峰标注不应遮挡样品主峰
- 不同候选相用不同颜色
- 样品谱始终作为主图层，参考峰作为辅助层
- 若候选相太多，优先只展示 2–4 个最重要候选

## 参考峰覆盖的推荐顺序

对于 316Ti 在 LBE 腐蚀后的样品，优先建议覆盖：

1. 奥氏体基体（fcc）
2. Magnetite / Fe3O4
3. Fe-Cr / Ni-Fe spinel
4. Cr2O3
5. 必要时再加 TiO2 / TiN

## 输出文件建议

- `sample_plot.png`：单谱图
- `stacked_plot.png`：多谱图叠图
- `sample_with_refs.png`：样品谱 + 参考峰覆盖图
- `peaks.csv`：自动提取的峰表

## 使用边界

- 自动峰标注属于辅助分析，不等于自动定相
- 参考峰覆盖仅用于支持/冲突比较，不等于数据库确认
- 图中峰位显示应与文字分析结论保持一致
- 不要为了图好看而标出没有证据支持的峰

## 技能规则补充

- 所有检测到的有效峰都要识别并列入峰表，不只列主峰。
- 每个峰都应尽量给出当前解释状态：已匹配、可能匹配、未匹配。
- 对暂时匹配不到的峰，不要强行归相；可以先不纳入候选相结论。
- 但必须明确告诉用户：哪些峰目前仍未匹配，它们的 2θ 位置是什么，以及这些未匹配峰会降低当前结论置信度。
- 如果某个候选相只能解释部分峰，也必须同步说明还有哪些峰未被该候选相解释。
