---
name: xrd-auto-analysis
version: 1.0
description: 面向材料分析场景的 XRD 自动分析 skill。用于读取 XRD 原始数据、提取峰、比较候选相、组织证据链并输出结构化判断与作图结果，强调已匹配/可能匹配/未匹配峰都必须明确交代。
---

# XRD Auto Analysis Skill

## 1. 核心定位

本 skill 用于处理 XRD 相关任务，包括：

- 读取原始 XRD TXT / CSV
- 提取峰位与峰质量信息
- 对照候选相参考峰进行比较
- 输出支持峰 / 缺失峰 / 冲突峰 / 未解释峰
- 生成单谱图、stacked plot、参考峰 overlay 图

它不是：

- 完整数据库检索器
- 全自动物相确认器
- 定量精修软件替代品

默认原则：

> 先读数据，再识别峰；先讲证据，再讲判断；已匹配峰、可能匹配峰、未匹配峰都必须交代清楚。

## 2. 支持输入

支持以下输入：

- SmartLab 导出的 TXT 数据
- 常规两列 XY 数据
- 峰表 CSV
- 谱图截图或 PDF 图像
- 用户提供的参考峰位列表
- 本地 CIF 结构参考库

输入路由规则：

1. 原始数据优先：先提取头信息和 XY，再识别峰。
2. 仅峰表时：直接做候选相比对。
3. 仅截图/PDF 图像时：只做粗略定性分析，不输出伪高精度峰位。
4. 用户提供候选相或参考峰时：优先做定向比对。

## 3. 核心分析流程

推荐流程：

```text
原始数据 / 峰表 / 参考峰
→ 峰识别
→ 峰质量评估
→ 候选相比对
→ 输出峰状态表
→ 输出结构化结论
→ 按需作图
```

硬规则：

1. 不能只列最强峰。
2. 所有有效峰都应入表。
3. 未匹配峰不能忽略，也不能为了好看硬归相。
4. 单峰禁止高置信认相。
5. 强未解释峰存在时，整体结论不得升到高置信。
6. 强参考峰缺失时，候选相置信度必须下降。

## 4. 置信度判断原则

推荐置信度标签：

- 高置信支持
- 中置信倾向
- 低置信可能
- 暂不支持

高置信通常至少要求：

1. 有原始 XY 数据。
2. 峰检测方法可追溯。
3. 同一候选相或合理相组合至少匹配 3 个有效峰。
4. RMS Δ2θ 通常 ≤ 0.15°，或已有合理偏移解释。
5. 没有强未匹配峰。
6. 与样品背景、元素组成、处理历史相容。

## 5. 当前目录中的主要实现资产

### 主说明文件
- `xrd-auto-analysis-技能说明.md`
- `xrd-auto-analysis-内部说明.md`

### 峰质量脚本
- `scripts/xrd_peak_quality_checker.py`

### 候选相比对脚本
- `scripts/xrd_confidence_matcher.py`

### 通用作图脚本
- `plot_xrd_generic.py`

### 参考峰模板
- `configs/reference_peaks_template.csv`

### 结构参考库
- `Structures/`
- `Structures_cif/`

## 6. 作图能力

当前支持：

1. 单谱图 + 自动峰位标注
2. 多谱图 stacked plot
3. 样品谱 + 候选相参考峰 overlay

对应入口：

```bash
python plot_xrd_generic.py single <input> <output> --label <name>
python plot_xrd_generic.py stacked <output> <label=path> <label=path> ...
python plot_xrd_generic.py overlay <input> <output> --label <name> --ref "phase=43.3,50.4"
```

## 7. 输出契约

推荐输出结构：

1. 数据概况
2. 仪器与采集信息
3. 峰表
4. 候选相分析
5. 已匹配 / 可能匹配 / 未匹配峰
6. 当前最可能解释
7. 置信度与为什么不是更高置信度
8. 后续建议

## 8. 边界条件

- 不能把用户提供的参考卡比对说成数据库确认。
- 截图输入只能做粗略分析。
- 未匹配峰必须明确告诉用户。
- 校正只能建立在高置信主峰归属前提下。
- 作图是辅助分析，不是定相本身。
