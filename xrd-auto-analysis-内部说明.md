# XRD 自动分析助手 · 内部说明

这份文档面向技能内部实现与后续维护，不是用户宣传文案。

目标是固定这套技能的触发条件、输入路由、分析顺序、输出契约、作图能力和边界条件，避免后续实现时漂移成“随意猜相”。

---

## 1. 技能定位

本技能是：

> 一个面向 XRD 判读、候选相比较、结果汇报与配图的自动分析助手。

不是：
- 完整标准数据库检索器
- 全自动物相鉴定器
- 定量精修软件替代品

本技能的核心价值：
- 帮用户读取 XRD 数据
- 识别尽可能完整的峰
- 组织证据链
- 区分已匹配 / 可能匹配 / 未匹配峰
- 输出诚实、结构化的判断
- 提供作图能力辅助汇报

---

## 2. 触发条件

当用户出现以下需求时触发：

- “分析 XRD”
- “识别物相”
- “读取 XRD txt/csv”
- “根据峰位判断是什么相”
- “给我看这张 XRD 谱图”
- “帮我比对标准卡 / 参考峰”
- “帮我把 XRD 画出来 / 叠图 / 标峰”
- “根据 CIF / PDF 标准卡判断候选相”

若用户同时提供：
- 样品背景
- 腐蚀环境
- 元素组成
- 候选相名单
- 标准卡 PDF / CIF 参考库

应优先使用这些上下文缩小候选相空间。

---

## 3. 输入路由

### A. 原始数据（TXT / CSV）
处理方式：
1. 读头信息
2. 识别数据区
3. 提取峰
4. 输出峰表
5. 做候选相分析
6. 如需要则作图

### B. 仅峰表
处理方式：
1. 规范化单位和字段
2. 用峰表做候选相比较
3. 给出支持/冲突/未解释峰

### C. 谱图截图 / PDF 图像
处理方式：
1. 只做定性或粗略峰判断
2. 不输出伪高精度峰位
3. 主动建议补原始数据

### D. 候选相 / 文献参考峰位
处理方式：
1. 定向比对
2. 看命中峰、缺失峰、冲突峰
3. 给当前置信度

### E. PDF 标准卡 / 参考卡片
处理方式：
1. 提取相名、卡号、峰位、强度、辐射源
2. 如清晰可读，整理成参考峰表
3. 如扫描模糊，只做有限比对
4. 必须声明：结论仅基于用户提供卡片，不代表完整数据库检索

### F. CIF 结构参考库
处理方式：
1. 优先通过索引文件检索候选相
2. 用相名、化学式、目录分类缩小范围
3. 将 CIF 作为候选相的本地参考来源，而不是直接宣称“数据库确认”

---

## 4. 分析顺序

标准顺序如下。

### Step 1. 数据概况
先说明：
- 当前输入类型
- 信息完整度
- 能做到哪一级分析
- 当前限制是什么

### Step 2. 读取采集信息
尽量提取：
- 靶材 / X-ray source
- 波长
- 扫描范围
- 步长
- 电压 / 电流
- 扫描模式
- 数据列定义

### Step 3. 峰识别
默认流程应改为：先提峰，再判相。

优先使用：
- `xrd/scripts/xrd_peak_quality_checker.py`

默认提取峰的三要素与质量信息：
- 峰位
- 峰强
- 峰形
- prominence
- SNR 估计
- quality_flag

峰形至少要识别：
- 尖峰
- 展宽峰
- 肩峰
- 疑似重叠峰

不再推荐把固定 `min_prominence` 阈值当作唯一提峰依据。

### Step 4. 所有有效峰入表
这是硬规则：

- 不能只列最强峰
- 所有检测到的有效峰都应列入峰表
- 每个峰都必须有当前状态标签

状态标签只能用：
- 已匹配
- 可能匹配
- 未匹配

### Step 5. 优先确定高置信主峰
如果最强峰能高置信归属于某个主相，应优先固定这个解释。

例如：
- 316Ti 样品中可优先把最强峰解释为奥氏体基体主峰
- 再基于主峰做简单 2θ 校正

### Step 6. 校正后重看小峰
若主峰可高置信归属，应允许做一阶零点平移校正，然后：
- 重新检查弱峰
- 重新比较小峰与候选相参考峰的偏差
- 小峰判断以校正后结果优先

### Step 7. 候选相比对
优先使用：
- `xrd/scripts/xrd_confidence_matcher.py`

对每个候选相，至少输出：
- 支持峰
- 冲突峰
- 缺失峰
- 未解释峰
- 当前置信度
- 为什么不是更高置信度

输出字段至少包括：
- `matched_count`
- `matched_ref_coverage`
- `matched_obs_coverage`
- `rms_delta_2theta`
- `missing_strong_refs`
- `unmatched_strong_obs`
- `matches`
- `why_not_higher`

不能只展示命中的峰，还必须明确：
- 候选相哪些强参考峰没出现
- 实验谱里哪些强峰仍未解释

### Step 8. 未匹配峰必须明确告知
这是硬规则：

- 未匹配峰不能忽略
- 未匹配峰不能为了好看硬归相
- 必须明确告诉用户：
  - 哪些峰未匹配
  - 它们的峰位是多少
  - 这会如何影响置信度

---

## 5. 置信度硬规则

### 1. 单峰禁止高置信
只凭 1 个峰，最多写“低置信可能”。

尤其是以下易重叠区域，禁止因为单峰相似就直接认相：
- 35° 附近
- 43–45° 附近
- 50–52° 附近

### 2. 图片输入限制置信度上限
若只有截图、论文图或 PDF 图像，没有原始 XY 数据：
- 最高只能给“低—中置信”
- 不输出伪精确峰位

### 3. 强未匹配峰压低整体置信度
若实验谱仍存在强未解释峰：
- 整体结论不得高于“中置信倾向”
- 必须列出峰位和相对强度

### 4. 强参考峰缺失时必须降级
若候选相理论强峰应出现但实验中缺失：
- 必须列出缺失强峰
- 必须降低该候选相置信度
- 不能只呈现匹配峰

### 5. 高置信支持的最低条件
通常需要同时满足：
1. 原始 XY 数据可用
2. 峰检测参数可追溯
3. 校准状态已说明
4. 同一候选相或合理相组合至少 3 个有效峰匹配
5. RMS Δ2θ 通常 ≤ 0.15°，或已有合理偏移解释
6. 没有强未匹配峰
7. 与元素、环境、处理历史相容
8. 最好有独立证据支持

---

## 6. 候选相判断规则

### 主原则
- 先看主峰，再看次峰
- 先看能解释多少峰，再看单个峰像不像
- 若某候选相只能解释部分峰，不能直接认定
- 若多个候选相共享主峰，必须用次峰和未匹配峰继续筛

### 优先级顺序
当样品背景清楚时，优先按背景约束筛候选相。

例如对于“316Ti 在 LBE 中腐蚀的样品”：

1. 先考虑基体残余峰
2. 再考虑 Fe 基氧化物 / magnetite / spinel
3. 再考虑 Cr2O3
4. 再考虑 Ti 相关相
5. 最后才考虑 Pb/Bi 相关残留相

### 输出语言要求
不能说：
- “就是某相”
- “已确认”

除非证据非常充分。

优先说：
- “高置信支持”
- “更倾向于”
- “可能为”
- “当前仍待验证”

---

## 6. 作图能力

本技能当前应支持以下三类作图模式。

### 模式 1：单谱图 + 自动峰位标注
输入：
- 单个 TXT/CSV

输出：
- 谱图 PNG
- 自动峰表 CSV

要求：
- 识别主峰和有效小峰
- 图中标注与峰表一致

### 模式 2：多谱图 stacked plot
输入：
- 多个 TXT/CSV
- 样品标签

输出：
- stacked plot PNG

要求：
- 统一 x 范围
- 归一化
- 纵向错位
- 标签清晰

### 模式 3：样品谱 + 候选相参考峰位覆盖
输入：
- 样品谱
- 一组候选相参考峰

输出：
- overlay PNG

要求：
- 样品谱作为主图层
- 参考峰作为辅助层
- 不同候选相使用不同颜色
- 不允许图中标注与文字分析脱节

---

## 7. 当前项目中的实现资产

### 参考脚本
- `xrd/plot_xrd_generic.py`

功能：
- `single`
- `stacked`
- `overlay`

### 峰质量脚本
- `xrd/scripts/xrd_peak_quality_checker.py`

### 置信度匹配脚本
- `xrd/scripts/xrd_confidence_matcher.py`

### 参考峰模板
- `xrd/configs/reference_peaks_template.csv`

### 参考说明
- `xrd/xrd_plotting_notes.md`

### CIF 总索引
- `xrd/Structures_cif/_cif_index.tsv`

### 316Ti 候选相清单
- `xrd/Structures_cif/_316ti_candidate_refs.md`

### 当前样品输出
- `xrd/output_xrd/316Ti-2_single.png`
- `xrd/output_xrd/316Ti-2_peaks.csv`
- `xrd/output_xrd/316Ti-2_overlay.png`
- `xrd/output_xrd/316Ti-2_overlay_lbe_candidates.png`
- `xrd/output_xrd/316Ti-2_corrected.png`

---

## 8. 输出契约

技能输出必须尽量稳定，推荐结构如下：

### 1. 数据概况
- 输入类型
- 数据完整度
- 当前限制

### 2. 仪器与采集信息
- 源 / 波长
- 扫描范围
- 步长
- 电压 / 电流

### 3. 峰表
每个峰至少要有：
- 峰位
- 强度
- 相对强度
- 峰形
- 当前状态

### 4. 候选相分析
每个候选相至少要有：
- 支持峰
- 冲突峰
- 缺失峰
- 未解释峰
- 当前置信度

### 5. 结论
- 当前最可能解释
- 已匹配峰概况
- 未匹配峰概况
- 置信度级别

### 6. 后续建议
- 是否需要标准卡
- 是否需要更多候选相
- 是否需要补 EDS/XPS/Raman/SEM
- 是否需要高分辨复测

---

## 9. 边界条件

### 边界 1
不能把用户提供的参考卡比对说成“数据库确认”。

### 边界 2
截图输入只能做粗略分析，不能输出伪高精度峰表。

### 边界 3
未匹配峰必须告知用户，不能省略。

### 边界 4
校正只能建立在高置信主峰归属的前提下，不能随意校。

### 边界 5
作图是辅助分析，不是定相本身。

---

## 10. 对 316Ti 在 LBE 中腐蚀样品的内部默认思路

对于这类样品，默认优先检查：

1. 奥氏体基体残余峰
2. magnetite / spinel 类腐蚀产物
3. Cr2O3
4. Ti 相关相
5. Pb/Bi 相关相

如果最强峰高置信属于奥氏体基体，则优先用它做简易校正，再重新检查小峰。

这类样品的关键，不是只看主峰，而是要把：
- 30° 左右的小峰
- 35° 左右的小峰
- 62° 左右的小峰

在校正后重新比较。

---

## 11. 维护原则

后续如果继续扩展本技能，应优先增加：

1. 更稳定的峰检测阈值控制
2. 从 CIF 自动导出参考峰位
3. 峰匹配状态表自动生成
4. PDF 标准卡的半自动结构化提取
5. 面向腐蚀体系的候选相模板

但不应牺牲当前这条主线：

> 所有峰都要识别；不能硬归相；未匹配峰必须明确告诉用户。
